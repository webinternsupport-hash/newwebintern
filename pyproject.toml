[project]
name = "webintern"
version = "1.0.0"
description = "Web Intern Platform"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0.0",
    "requests>=2.31.0",
    "supabase>=2.3.0",
    "python-dotenv>=1.0.0",
    "reportlab>=4.0.0",
    "qrcode>=7.4.2",
    "pillow>=10.0.0",
    "gunicorn>=21.0.0",
]

[tool.vercel]
entrypoint = "api.index:app"
